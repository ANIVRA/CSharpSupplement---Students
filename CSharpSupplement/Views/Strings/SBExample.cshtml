@{
    ViewBag.Title = "C# Fundamentals";
}
<h2>@ViewBag.Title.</h2>


<h3>@ViewBag.Message</h3>

<div class="row">
    <div class="label col-xs-10">
        <pre  style="color:black; font-size:x-large"> 
        Create a string composed of numbers 0 - 9
        
            for (int i = 0; i < 10; i++)
{
                sb.Append(i.ToString());
}
       
        This loop displays 0123456789
        </pre>
    </div>
</div>
<hr />
<form>
    <div class="row">
        <div class="col-xs-6">
            <div class="row">
                <input type="submit" class="btn btn-default col-xs-6 well" style="color:black; font-size:xx-large" value="build" name="str" onsubmit="@Url.Action("SBExample","Strings")" />
            </div>

            <div class="row">
                <input type="submit" class="btn btn-default col-xs-6 well" style="color:black; font-size:xx-large" value="replace" name="str" onsubmit="@Url.Action("SBExample","Strings")" />
            </div>

            <div class="row">
                <input type="submit" class="btn btn-default col-xs-6 well" style="color:black; font-size:xx-large" value="reverse" name="str" onsubmit="@Url.Action("SBExample","Strings")" />
            </div>
        </div>
        <div class="col-xs-6">
            <div class="row">
                <div class="col-xs-12 text-center" style="padding-top:15px; font-size:xx-large">
                    Result = @ViewBag.Result
                </div>
            </div>
        </div>
    </div>
</form>




